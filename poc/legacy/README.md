# Legacy POC files

These scripts were migrated from `C:\tmp` on 2026-07-03 so the temporary
directory could be cleaned safely. They preserve earlier experiments and may
contain obsolete hard-coded paths or request formats. Current work should use
the modules directly under `poc/`.
