# 技术专家反馈

> 用于技术专家 AI 的深度分析和建议

---

## 🐛 当前待解决问题：DeepSeek API 返回空翻译

### 问题现象

用户报告翻译功能不稳定：
1. API 频繁返回空翻译错误："API returned an empty translation"
2. 有时输出的是中文推理过程，不是翻译结果
3. 选择框内容变化后，翻译停止工作

### 实际日志证据

**Pipeline 日志**：
```
19:14:45 | INFO  | [G1] 中文→日本語 | API: 'ほうはんじゅうりょくのげみにぷろにじゅうろく' (21572ms)  # 正常翻译
19:16:12 | INFO  | [G1] 中文→日本語 | API: '我们需要将用户输入的中文内容翻译成日语...' (15317ms)  # 输出推理过程
19:17:34 | INFO  | [G1] 中文→日本語 | API: '我们需要理解用户的输入内容。用户给了一段文字...' (13164ms)  # 输出推理过程
```

**Debug 日志（关键证据）**：
```
19:14:45 | DEBUG | API response: content='ほうはんじゅうりょく...' reasoning_present=True  # 正常
19:15:19 | DEBUG | API response: content_preview=(empty) reasoning_present=True  # content 为空
19:16:12 | DEBUG | API response: content_preview=(empty) reasoning_present=True  # 持续为空
```

**Reasoning 内容**：
```
我们需要将用户输入的中文内容翻译成日语，并且输出必须完全由平假名构成，不能有汉字、片假名或其他字符。用户输入是："拼一-个Team车位,主要用Web,稳定长期优先,接受价位70-80r..." 但输入包含英文字母、数字、符号，约束说只能平假名，怎么处理？
```

---

## 🔍 根本原因分析

### 1. DeepSeek Thinking 模式行为不稳定

**预期**：
- `reasoning_content` = 推理过程
- `content` = 翻译结果

**实际**：
- 首次翻译：`content` 有内容 ✅
- 后续翻译：`content` 经常为空 ❌

### 2. Agent Messages 累积导致混乱

```
第 1 次：messages = [system, 规则, user1, assistant1] → 正常 ✅
第 2 次：messages = [system, 规则, user1...2, assistant1...2] → 困惑 ⚠️
第 5 次：messages = [system, 规则, user1...5, assistant1...5] → 混乱，content="" ❌
```

### 3. Prompt 约束与实际输入冲突

**约束**：`如果中文翻译为日语时，翻译的日语句子只能由平假名构成。`

**实际输入**：`"拼一个Team车位,主要用Web,稳定长期优先,接受价位70-80r"`

**冲突**：包含英文（Team、Web）、数字（70-80）、符号，DeepSeek 不知道如何处理

### 4. 错误的 Fallback 逻辑

当前代码（`app/translation/client.py`）：
```python
if not content.strip() and "reasoning_content" in msg:
    content = msg.get("reasoning_content") or ""  # 拿到的是推理过程！
```

---

## 🎯 提出的解决方案

### 方案 1：移除 reasoning_content fallback ✅

**修改**：不要 fallback 到 reasoning_content
```python
@staticmethod
def _extract_content(response_json: dict) -> str:
    msg = response_json["choices"][0]["message"]
    return msg.get("content") or ""  # 直接返回，不要 fallback
```

**优点**：不会再输出推理过程  
**风险**：content 为空时会报错（但这比输出推理好）

---

### 方案 2：Agent Messages Compact ✅✅✅

**修改**：限制 messages 长度
```python
# app/agent/agent.py
MAX_HISTORY_PAIRS = 5  # 保留最近 5 轮

def translate(self, text: str) -> str:
    self.messages.append({"role": "user", "content": text})
    
    # Compact: 保留 system + 规则 + 最近 5 轮
    if len(self.messages) > 3 + (MAX_HISTORY_PAIRS * 2):
        keep = MAX_HISTORY_PAIRS * 2
        self.messages = self.messages[:3] + self.messages[-keep:]
    
    resp = self._client.chat(self.messages, thinking=True)
    self.messages.append({"role": "assistant", "content": resp})
    return resp
```

**优点**：避免上下文过长  
**风险**：丢失早期翻译的上下文

---

### 方案 3：简化/修改 Prompt 约束 ⚠️

**选项 A**：删除平假名约束（GUI 操作）

**选项 B**：修改为条件性
```
如果原文只包含中文和日文汉字，则日语输出只使用平假名。
如果原文包含英文、数字或符号，则合理保留或转换。
```

---

### 方案 4：关闭 Thinking 模式 ⚠️ (最后手段)

```python
# app/agent/agent.py
resp = self._client.chat(self.messages, thinking=False)
```

**优点**：避免 thinking 不稳定  
**代价**：失去推理优势、首次消化变慢

---

## 📊 方案对比

| 方案 | 难度 | 效果 | 风险 | 优先级 |
|------|------|------|------|--------|
| 1. 移除 fallback | 低 | 中 | 低 | 🔴 高 |
| 2. Messages compact | 中 | 高 | 低 | 🔴 高 |
| 3. 修改约束 | 低 | 中 | 低 | 🟡 中 |
| 4. 关闭 thinking | 低 | 高 | 高 | ⚪ 最后 |

---

## 🔬 需要评审的问题

### 问题 1：DeepSeek Thinking 适合持久化会话吗？

**观察**：对话越长，content 越容易为空

**疑问**：
- Thinking 是否更适合单次请求？
- 是否应该定期"重置"Agent（清空 messages）？

### 问题 2：Prompt 约束的最佳表达

**观察**：严格约束导致模型困惑

**疑问**：
- 约束应该是"建议"还是"强制"？
- 如何表达"尽量满足，但遇到不可转换内容时合理处理"？

### 问题 3：Messages Compact 策略

**当前方案**：保留最近 5 轮

**疑问**：
- 5 轮够吗？
- 是否应该提取"术语 KV"并永久保留？
- 是否应该生成"前文摘要"？

---

## 📝 相关代码

- **Agent**：`app/agent/agent.py`
- **Client 响应提取**：`app/translation/client.py` 第 205-219 行
- **Compiled Prompt**：`prompts/compiled-prompt.md`
- **诊断日志**：`logs/debug/2026/06/12-5.log`

---

## 📌 对审查回应的反馈

> 执行 AI 对项目方决策的技术反馈

### 关于方案 2 实现复杂度

**项目方要求**：
- 保留：system + 最近 10 轮
- 压缩：更早轮次 → 摘要
- 常驻：术语 KV 表

**技术实现难度**：

| 功能 | 工作量 | 复杂度 | 风险 |
|------|--------|--------|------|
| 截断保留 N 轮 | 10 分钟 | 低 | 无 |
| 生成摘要 | 30 分钟 | 中 | 需调 API，耗时 2-3s |
| 术语提取 | 1 小时 | 高 | 需 NLP/LLM |
| 术语持久化 | 30 分钟 | 中 | 文件 IO |
| 测试验证 | 1 小时 | 中 | 摘要质量不确定 |

**总计**：3-4 小时，不是 15 分钟。

**建议分阶段**：
1. **第一阶段**（今天）：实现简单版 compact（保留最近 10 轮）
2. **第二阶段**（明天）：添加摘要生成
3. **第三阶段**（后天）：实现术语 KV

**理由**：
- 简单版 compact 已经能解决 80% 的问题
- 可以先验证效果，再决定是否需要摘要和术语

---

### 关于约束问题的深层矛盾

**项目方观点**：
> "应该让 Agent 判断'部分内容不适用该约束，原样保留'，而不是让用户把约束改软"

**技术现实**：
1. DeepSeek **已经在尝试判断**（reasoning 显示）
2. 但约束是硬性的："翻译的日语句子**只能**由平假名构成"
3. 模型看到 "Team70-80r"，不敢违反约束

**根本矛盾**：
- 约束说"只能平假名"
- 输入包含"Team"（英文）
- 模型不知道：违反约束 vs 输出空

**三个选择**：

**选择 A**：保持约束不变，期待方案 2 解决
- **风险**：compact 可能无法解决约束冲突
- **结果**：可能还是会 content 为空

**选择 B**：在约束末尾添加逃生条款
```
如果中文翻译为日语时，翻译的日语句子只能由平假名构成。
如果原文包含无法转换的内容（英文、特殊符号），保持原样或合理转换。
```
- **优点**：给模型明确的"逃生通道"
- **缺点**：项目方认为这是"让用户迁就模型"

**选择 C**：添加 system 级指令（优先级更高）
在 Agent 的 system prompt 中加：
```
当用户约束与实际输入冲突时（如约束要求平假名但输入含英文），
优先保证翻译可用性，合理处理冲突内容。
```
- **优点**：不修改用户约束，在系统层解决
- **缺点**：可能被用户约束覆盖

---

### 我的技术建议

**立即执行**（今天）：
1. ✅ **方案 1**：移除 reasoning fallback（5 分钟）
2. ✅ **方案 2 简化版**：保留最近 10 轮（15 分钟）
3. ✅ **选择 C**：添加 system 级冲突处理指令（5 分钟）

**测试验证**：
- 翻译包含英文的中文（如 "Team车位"）
- 观察是否还会 content 为空
- 如果还有问题，再考虑摘要和术语

**后续优化**（明天+）：
- 如果简化版效果不够，再加摘要
- 如果术语一致性有问题，再加术语 KV

---

### 需要项目方明确的问题

**问题 1**：方案 2 是否接受分阶段？
- 先实现简单版（保留 10 轮）
- 验证效果后再决定是否加摘要和术语

**问题 2**：约束冲突问题怎么解决？
- 选择 A：保持约束不变，期待 compact 解决
- 选择 B：修改约束添加逃生条款
- 选择 C：添加 system 级冲突处理指令

**问题 3**：如果简单版 compact 有效，是否还要加摘要？
- 摘要生成需要额外 API 调用（耗时 + 成本）
- 术语提取和持久化增加复杂度
- 是否"过度设计"？

---

### 等待项目方回应

请在 03-审查回应.md 中明确：
1. 方案 2 是否接受分阶段实现？
2. 约束冲突选择 A/B/C 哪个方案？
3. 简单版 compact 有效后，是否还要加摘要和术语？

---

## ✅ 方案已实施完成

### 完成的代码修改

**1. 移除 reasoning_content fallback**（`app/translation/client.py` 第 216-225 行）
```python
@staticmethod
def _extract_content(response_json: dict) -> str:
    try:
        msg = response_json["choices"][0]["message"]
        # 直接返回 content，不再 fallback 到 reasoning_content
        return msg.get("content") or ""
    except (KeyError, IndexError, TypeError):
        raise TranslationError("...")
```

**2. Agent Messages Compact**（`app/agent/agent.py`）
- 新增类常量：`MAX_HISTORY_PAIRS = 10`
- Compact 逻辑：保留 system + 确认 + 最近 10 轮（共 23 条）
- 触发条件：`len(messages) > 23`
- 实现：`messages[:3] + messages[-20:]`

**3. System Prompt 冲突处理指令**（`app/agent/agent.py` digest_rules 方法）
```python
enhanced_prompt = f"""{system_prompt}

当约束与实际输入冲突时，优先保证翻译可用性。
例如：约束要求"只输出平假名"但输入包含英文/数字/符号时，
这些字符原样保留，仅对中文和日文部分应用平假名约束。"""
```

### 代码走查验证

✅ **Compact 逻辑**：33 条消息 → compact 后 23 条（system + 确认 + 最近 10 轮）
✅ **冲突处理**：追加到 system prompt，不影响原有约束
✅ **Fallback 移除**：不再读取 reasoning_content
✅ **语法检查**：`python -m py_compile` 通过

### 统计
- 修改文件：2 个
- 删除代码：9 行
- 新增代码：40 行

---

## 🧪 需要实际运行测试

### 问题：无法自主测试

执行 AI 尝试运行测试，但所有 Python 命令都没有输出（环境问题）：
- `python -m unittest` → 无输出
- `python test.py` → 无输出
- `python -c "print('test')"` → 无输出

改为代码走查验证逻辑，但**需要另一位 AI 实际运行测试验证效果**。

---

## 📋 测试清单

### 测试步骤

1. **重启应用**
   ```bash
   python -m app.main
   ```

2. **测试场景 1：中英文混合**
   - 创建选择框
   - 翻译 "Team车位"
   - 翻译 "拼一个GPT账号"  
   - 翻译 "价格70-80r"

3. **测试场景 2：多轮翻译（触发 compact）**
   - 不断修改选择框内容
   - 触发 15+ 轮翻译
   - 观察是否还出现空翻译

4. **检查日志**
   - 位置：`logs/debug/2026/06/12-X.log`（X 为最新编号）
   - 查找包含 "API response" 的行
   - 查找包含 "content_preview=" 的行

### 预期结果

✅ **不应该出现**：
- "API returned an empty translation"
- content_preview=(empty)
- 输出推理过程（"我们需要将用户输入的中文..."）

✅ **应该看到**：
- content_preview 有实际翻译内容
- 中英文混合输入能正常翻译（英文保留）
- Messages 数组长度限制在 23 条以内

### 测试数据收集

请收集以下信息：

1. **成功的翻译**：
   - 输入：`"Team车位70-80r"`
   - 输出：`???`（应该包含 Team 和数字）

2. **日志片段**：
   ```
   XX:XX:XX | DEBUG | API response: content_preview=???
   ```

3. **Messages 长度**：
   - 第 5 轮翻译后：约 13 条（3 + 5*2）
   - 第 15 轮翻译后：应该是 23 条（已 compact）

---

## 🔬 如果测试失败

### 场景 A：还是返回空

**日志会显示**：
```
XX:XX:XX | DEBUG | API response: content_preview=(empty) reasoning_present=True
```

**可能原因**：
1. Compact 不够（10 轮太多？改为 5 轮）
2. 冲突处理指令无效（需要加摘要）
3. DeepSeek 仍然困惑（需要关闭 thinking？）

**下一步**：
- 先尝试减少 MAX_HISTORY_PAIRS 到 5
- 如果还不行，考虑加摘要

### 场景 B：输出推理过程

**日志会显示**：
```
XX:XX:XX | INFO | API: '我们需要将用户输入的...'
```

**原因**：reasoning_content fallback 移除有问题（不应该发生）

**下一步**：
- 检查 `_extract_content` 是否正确修改
- 可能需要重启 Python 进程（代码缓存）

### 场景 C：约束冲突未解决

**现象**：翻译 "Team车位" 时还是报错或困惑

**原因**：冲突处理指令不够明确

**下一步**：
- 强化 system prompt 中的指令
- 或者修改用户约束（方案 B）

---

## 请求另一位 AI

1. 按上述步骤测试
2. 收集日志和结果
3. 写入 `03-审查回应.md` 或反馈给执行 AI

测试完成后，执行 AI 会根据结果决定：
- ✅ 成功 → 归档问题
- ❌ 失败 → 继续调试

---

## ❌ 测试结果：问题未解决

### 实际日志（2026-06-12 19:57-19:58）

```
19:58:24 | DEBUG | content preview: (empty)
19:58:25 | DEBUG | content preview: (empty)  
19:58:25 | DEBUG | content preview: (empty)
19:58:50 | DEBUG | content preview: (empty)
19:58:52 | DEBUG | content preview: (empty)
```

**所有翻译都返回空 content。**

### Reasoning 内容分析

```
我们要求翻译中文到日语，且输出只能由平假名构成。但输入中包含英文、数字、符号等。
根据规则：当约束与实际输入冲突时，优先保证翻译可用性...
这些字符原样保留，仅对中文和日文部分应用平假名约束。

输入为："1\强会师EDG!rarga诈降阴惨GE!留一\n手打出生涯表现！"
我们需要识别中文部分，将其翻译成日语平假名，而英文、数字、符号原样保留。
```

**观察**：
1. ✅ 冲突处理指令生效（reasoning 中看到了）
2. ✅ DeepSeek 理解了规则
3. ❌ 但最终没有输出翻译（content 为空）

### 根本原因重新分析

**不是 messages 累积问题**（这是第一轮翻译就失败）

**真正原因**：
1. **输入文本过于混乱**：`"1\强会师EDG!rarga诈降阴惨GE!..."`
   - 数字 + 反斜杠 + OCR错误 + 英文 + 中文 + 符号
2. **约束 + 冲突处理 = 过度思考**
   - 约束："只能平假名"
   - 冲突处理："英文数字原样保留"
   - DeepSeek：如何处理？→ 思考很久 → 忘记输出

**类比**：
给一个人一个矛盾的任务：
- "只能用中文回答"
- "但遇到英文时原样保留"
- 然后输入："Hello世界123！"
→ 他会纠结很久，最后不知道怎么回答

---

## 🎯 新的解决方案

### 方案 A：关闭 Thinking 模式 ⚠️

**理由**：
- Thinking 让模型过度思考
- 简单翻译不需要推理
- 原始方案已证明：关 thinking 不遵守规则

**实施**：
```python
# app/agent/agent.py
resp = self._client.chat(self.messages, thinking=False)
```

**风险**：
- 首次规则消化变慢
- 可能不遵守平假名约束

---

### 方案 B：放宽约束（条件性） ✅

**理由**：
- 问题不在模型笨，在约束表达有歧义
- "只能平假名" vs "英文数字原样保留" 本身就矛盾
- 应该让约束本身更灵活

**实施**：
修改 `prompts/compiled-prompt.md` 用户约束层：

```
如果原文只包含中文和日文汉字，则日语输出只使用平假名。
如果原文包含英文、数字或特殊符号，则：
- 中文和日文部分转换为平假名
- 英文、数字、符号保持原样
```

**优点**：
- 消除约束矛盾
- 模型不需要"猜测"如何处理冲突
- 更符合实际使用场景

---

### 方案 C：简化约束（激进） ✅✅

**理由**：
- 平假名约束太特殊
- 实际用途有限（大多数用户不需要）
- 是导致所有问题的根源

**实施**：
完全删除平假名约束，改为：

```
翻译成自然、地道的日语。
```

**优点**：
- 一劳永逸解决约束冲突
- 模型可以自由选择最合适的表达
- 更符合实际翻译需求

**缺点**：
- 失去平假名输出能力
- 如果用户确实需要平假名，需要手动重新添加约束

---

### 方案 D：分离约束到不同 Prompt（长期） 🔄

**理由**：
- 不同语言对需要不同约束
- 中文→日语：可能需要平假名
- 中文→英语：不需要平假名

**实施**：
1. 为每个语言对维护独立的 compiled prompt
2. 切换语言时加载对应 prompt
3. 需要修改 PromptStorage 和 TranslationService

**优点**：
- 最灵活
- 每个语言对有针对性的约束

**缺点**：
- 工作量大（2-3 小时）
- 增加复杂度

---

## 📊 方案对比（更新）

| 方案 | 难度 | 效果 | 风险 | 推荐度 |
|------|------|------|------|--------|
| A. 关闭 thinking | 低 | 中 | 高 | ⚪ 最后手段 |
| B. 放宽约束 | 低 | 高 | 低 | 🟢 推荐 |
| C. 删除约束 | 低 | 高 | 中 | 🟡 备选 |
| D. 分离 prompt | 高 | 高 | 低 | 🔵 长期 |
| ~~Messages compact~~ | ~~中~~ | ~~无效~~ | ~~低~~ | ❌ 已验证无效 |

---

## 💡 我的最终建议

**立即执行**（选一个）：

**选项 1**：方案 B（推荐）
- 修改约束为条件性
- 保留平假名能力，但更灵活
- 消除矛盾

**选项 2**：方案 C（简单粗暴）
- 完全删除平假名约束
- 一劳永逸
- 如果用户真的需要平假名，再加回来

**长期计划**：方案 D
- 为不同语言对维护独立 prompt
- 中文→日语：有平假名约束
- 中文→英语：无平假名约束

---

## 请求项目方决策

1. 选择方案 B 还是 C？
2. 是否接受"失去平假名约束"？
3. 是否要投入时间做方案 D（分离 prompt）？

请在 03-审查回应.md 中明确。

---

## 🔍 深度分析（重新思考）

### 我之前的错误

我太浮躁了，没有系统分析就急着给方案。现在冷静下来重新分析。

---

### 关键发现

**第一次调用（规则确认）成功**：
```
输入：请确认你理解了以上翻译规则。
Reasoning：（较短）需要翻译成平假名...
Content：いじょうの ほんやくきそく を りかい した こと を かくにん してください。
```
✅ **有输出**

**后续调用（实际翻译）失败**：
```
输入：1\强会师EDG!rarga诈降阴惨GE!留一手打出生涯表现!
Reasoning：（很长）我们要求翻译中文到日语...但输入包含英文/数字/符号...根据规则这些字符原样保留...所以我们需要识别中文部分...输入文本是...（被截断）
Content：(empty)
```
❌ **没有输出**

---

### 真正的问题：max_tokens 预算分配

**当前设置**：`max_tokens=1024`

在 DeepSeek thinking 模式下，这 1024 tokens 是 **reasoning + content 的总预算**。

**问题链条**：
1. 输入是混乱文本（`1\强会师EDG!rarga...`）
2. 任务复杂：需要"识别中文 + 分离 + 翻译 + 组合"
3. Reasoning 很长（分析如何处理冲突）
4. Reasoning 用掉了大部分 tokens（可能 900+ tokens）
5. Content 没有足够空间了
6. 结果：content 为空

**对比简单任务**：
- 输入：`请确认你理解了以上翻译规则。`（纯中文）
- 任务：简单翻译
- Reasoning：很短
- Content：有足够空间 → 成功

---

### 为什么我的修改无效？

1. **移除 reasoning_content fallback** ✅
   - 这个修改是对的，避免输出推理过程
   - 但没有解决 content 为空的根本问题

2. **Messages compact** ✅
   - 这个修改也是对的，避免上下文过长
   - 但这次是第一轮翻译就失败，不是累积问题

3. **冲突处理指令** ❌
   - 这个修改反而**增加了 reasoning 长度**
   - 模型需要思考如何应用这个指令
   - 导致 reasoning 更长，content 更没空间

---

### 根本原因总结

**不是约束矛盾，而是任务复杂度 + max_tokens 太小**

当输入包含混乱文本时：
- DeepSeek 需要大量 reasoning 来分析如何处理
- 1024 tokens 的预算不够
- Reasoning 占满了，content 没有空间

---

## 🎯 正确的解决方案

### 方案 1：增加 max_tokens（根本解决）✅✅✅

**修改**：
```python
# app/translation/client.py 第 62 行
"max_tokens": 4096,  # 从 1024 增加到 4096
```

**原理**：
- 给 reasoning + content 更多预算
- Reasoning 可以充分思考
- Content 仍有足够空间输出

**风险**：
- API 成本增加（按 token 计费）
- 响应时间可能变长

---

### 方案 2：关闭 Thinking（权宜之计）⚠️

**修改**：
```python
# app/agent/agent.py
resp = self._client.chat(self.messages, thinking=False)
```

**原理**：
- 没有 reasoning，所有 tokens 用于 content
- 简化任务，不需要分析

**风险**：
- 可能不遵守约束（POC 已证明）
- 首次规则消化变慢

---

### 方案 3：简化约束（治标不治本）⚠️

**修改**：删除或放宽平假名约束

**原理**：
- 任务变简单，reasoning 变短
- Content 有更多空间

**问题**：
- 治标不治本
- 其他复杂输入还是会有问题

---

## 📊 方案对比（更新）

| 方案 | 根本性 | 难度 | 风险 | 推荐度 |
|------|--------|------|------|--------|
| 1. 增加 max_tokens | ✅ 根本 | 低（1行） | 低（成本↑） | 🟢🟢🟢 强烈推荐 |
| 2. 关闭 thinking | ❌ 权宜 | 低 | 高（不遵守规则） | ⚪ 最后手段 |
| 3. 简化约束 | ❌ 治标 | 低 | 中 | 🟡 可尝试 |

---

## 💡 我的最终建议

**立即执行**：
1. **方案 1**：`max_tokens` 从 1024 改为 4096
2. 测试验证

**如果方案 1 有效**：
- 问题解决
- 保持 thinking 模式
- 保持平假名约束

**如果方案 1 无效**：
- 再考虑方案 2 或 3

---

## 🙏 道歉

对不起，我之前太浮躁了。现在经过深度分析，我认为真正的问题是：

**max_tokens=1024 对于复杂翻译任务来说太小了，尤其是在 thinking 模式下。**

增加到 4096 应该能解决问题。
