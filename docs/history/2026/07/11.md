# 2026-07-11 开发记录

> 依据 git 提交补记（当日落地多条主线改动）。部分工程叙述亦见 `docs/history/2026/07/03.md`（记录日与提交日不完全重合）。

## 提交一览

| Commit | 说明 |
|--------|------|
| `89cfc79` | chore: ignore AI tool dirs（`.codex` / `.agents` / `.playwright-mcp`） |
| `44934e5` | feat: 约束 Policy 编译与引用层（sidecar、runtime profile、reference 检索） |
| `11c5706` | feat: Agent 双模翻译与校验回退（Flash 快路径、Pro 重试、session/profile、反馈记忆） |
| `9a2cb6c` | fix: OCR 预热串行与选区/翻译窗稳定性（Paddle 锁、副屏坐标、重叠恢复） |
| `6613a94` | fix: GUI 后台测连与日志/打包路径（测连不堵主线程、日志与 PyInstaller） |
| `f397eb3` | chore: POC 迁入 `poc/`，清理根目录散落脚本 |
| `02e0162` | test: policy、引用层与 Agent/POC 相关单测 |
| `1bd5926` | docs: 2026-07 开发记录与协作总结 |
| `df44323` | chore: 移除 Playwright MCP 调试残留 |

## 要点

### Policy 与引用

- 用户自然语言约束可编译为本地白名单 **Policy sidecar**；不执行模型生成的代码/正则。
- 引用层支持术语/风格类检索与保护，减少 Flash 漏术语与格式漂移。

### Agent 双模

- 日常翻译：**Fast + thinking disabled**。
- 校验 hard 失败：可 **Pro thinking 重试**。
- Session / Profile：按语言对、prompt hash、模型元数据隔离；配置变化则重建。

### OCR / Overlay / GUI

- OCR 引擎串行访问与预热，降低多组并发卡死与首帧冷启动。
- 选区截图按所在屏坐标；翻译窗与采集区相交时回退自动停靠。
- 模型连接测试改后台线程；运行日志路径与打包配置整理。

### 仓库卫生

- POC 统一进 `poc/`；忽略本机 AI 工具目录；清理误提交的 Playwright 缓存/快照。

## 说明

- 当日提交信息为权威变更列表；细节与验证数字以 `07-03.md` 及对应 commit message 为准。
- 后续「启动时 Pro Profile 预热」「反馈即时刷新」等亦在此批 Agent/反馈相关提交中落地（见 07-03 文末补充节）。
