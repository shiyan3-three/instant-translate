# 2026-07-16 开发记录

## 工程基线

- 新增 `app/runtime.py`：启动前检查 Python 3.10–3.12。
- 新增 `app/secret_store.py`：Windows DPAPI 保护 API Key；`settings` 原子写 + 明文密钥迁移。
- 主进程单实例锁、日志与打包/CI 基线同步（含 `requirements-py312.lock`、release workflow）。

## 反馈记忆闭环

- `FeedbackStore` 强化事务 journal、巩固状态机与不可用降级（`allow_unavailable`）。
- 新增 `learning`（后台巩固队列）、`retrieval`（词法 + 权威包装）、`memory_policy`（自动记忆规范模板）。
- 翻译服务注入本地记忆并记录 provenance；Agent 侧记忆 JSON 注入与假术语包装修复。

## Overlay / GUI

- OCR/译文悬浮窗：**按文本自适应**宽高，不再跟死选择框宽度。
- 尺寸上限：**屏幕物理像素约 950×165**（逻辑尺寸 = 上限 / 所在显示器 DPR，并夹该屏可用区）。
- 编辑模式：译文下移 1.5×自身角栏高度，OCR 上移 1.5×主工具栏高度；偏移绝对值不叠加；拖拽保存基准坐标。
- 选区主工具栏对齐 mock：`源 → 目标` + 28px 操作图标；角栏与沉浸等交互加固。
- 主窗壳层/反馈页布局与样式继续对齐设计规范与 mock。

## 文档

- `docs/goat/15-后期技术债清单.md`
- `docs/goat/16-网页自动填入插件设计.md`（软件 OCR/译文 → 网页输入框；桌面桥 + 扩展草案，**未实现代码**）
- `docs/goat/13-决策记录.md` 增补网页填入决策
- `docs/communication/gui-mock/` 视觉沙盒与 implementer 规格

## 验证

```text
629 passed, 1 skipped, 102 subtests passed
```

（排除部分 `test_poc_*` 长时间 POC 用例时的主回归结果。）

## 后续

- 网页自动填入：仅文档与接口草案，实现待开。
- 多显示器 + 高 DPI 下再人工确认悬浮框物理尺寸是否贴近 950×165。
